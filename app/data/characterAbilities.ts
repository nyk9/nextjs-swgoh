type Abilities = [
    ability_type: string,
    name: string,
    image: string,
    description: string,
    is_zeta: boolean,
    is_omega: boolean,
    is_omicron: boolean,
    is_ultimate: boolean,
]

export type characterAbilities = {
    character_name: string,
}

const hoge: characterAbilities[] = [
    { character_name: "" }
]